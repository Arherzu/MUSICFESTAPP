@page
@model LoginModel

@{
    ViewData["Title"] = "Acceder";
}

<h1>@ViewData["Title"]</h1>
<div class="row">
    <div class="col-lg-6" style="float:none;margin:auto;">
        <section>
            <form id="account" method="post">
                <hr />
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Input.Email">Ingrese su Email</label>
                    <input asp-for="Input.Email" class="form-control rounded-pill bg-secondary text-white" />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Input.Password">Ingrese su Contraseña</label>
                    <input asp-for="Input.Password" class="form-control rounded-pill bg-secondary text-white" />
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <div class="checkbox">
                        <label asp-for="Input.RememberMe">
                            <input asp-for="Input.RememberMe" /> Recordarme</label>
                    </div>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn rounded-pill btn-dark btn-block">Iniciar sesión</button>
                </div>
                <div class="form-group">
                    <p>
                        <a id="forgot-password" asp-page="./ForgotPassword">Olvidaste tu contraseña?</a>
                    </p>
                    <p>
                        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Registrate</a>
                    </p>
                    <p>
                        <a id="resend-confirmation" asp-page="./ResendEmailConfirmation">Reenviar confirmación de email</a>
                    </p>
                </div>
            </form>
        </section>
    </div>
    
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
